# Auto Status MVP (Client)

## Prereqs
- Node 20+
- Angular CLI 19+

## Run
```bash
cd client
npm install
npm start
```

This uses `proxy.conf.json` to proxy `/api` to the Node server at `http://localhost:4000`.
