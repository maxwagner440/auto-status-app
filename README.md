# Auto Status MVP (Angular + NestJS + TypeORM + Postgres)

## Start API + Postgres (Docker)
From repo root:
```bash
docker compose up --build
```

API: http://localhost:4000

## Run Angular client
```bash
cd client
npm install
npm start
```

Client: http://localhost:4200
